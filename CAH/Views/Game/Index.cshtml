@{
  Layout = "_Layout";
}

@using CAH.Models;

@model CAH.ViewModels.CardsViewModel;

@{
  Random random = new Random();
  var randomBlackCard = Model.BlackCards[random.Next(Model.BlackCards.Count)];
  var randomWhiteCard = Model.WhiteCards[random.Next(Model.WhiteCards.Count)];
}

<h1>Censorship Against Humanity</h1>

<br><br>

<h2 id="card-h2">Random Black Card</h2>
<div class="blackCard">
  <!-- <p class="random-card">@randomBlackCard.Text</p> --> 
  <!-- lmw comment out above -->
  <p class="random-card">@ViewBag.InitialBlackCard</p>
</div>


<br><br>
<h2 id="card-h2">Your Cards</h2>

<div class="whiteCardContainer">
  <div class="whiteCard">
      <!-- <p class="random-card">@randomWhiteCard.Text</p> -->
      <!-- lmw comment out above -->
      <p class="random-card">@ViewBag.InitialWhiteCard</p>
  </div>
  


  @if (TempData["RandomWhiteCard"] != null)
  {
      <div class="whiteCard">
        <p class="random-card">@TempData["RandomWhiteCard"]</p> 
      </div>  
  }

</div>

@using (Html.BeginForm("Create", "Game", FormMethod.Post))
{
  <button type="submit">Generate second card</button>
}
<!-- @Html.ActionLink("Add another white card", "Create"); -->

<br><br><br>
<!-- <h2>White Cards</h2>
@foreach (WhiteCards whiteCards in Model.WhiteCards)
{
  <li>@whiteCards.Text</li>
}

<br><br><br>
<h2>Black Cards</h2>

@foreach (BlackCards blackCards in Model.BlackCards)
{
  <li>@blackCards.Text</li>
} -->